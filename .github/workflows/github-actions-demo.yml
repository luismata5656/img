name: Image Resize
run-name: Image Resize
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: scripts/
      - name: Resize Images in resize/ directory
        run: |
          bash ${GITHUB_WORKSPACE}/run_digest.sh
      - run: echo " This job's status is ${{ job.status }}."